Highlights

- ReduLink separates authorized residency, representation alignment, and byte economics.
- A 40.87M-record IBM trace bounds exact same-client warm bytes below request reuse.
- Pinned compressed-layer updates show ordinary whole-layer CAS dominates fine-grained reuse.
- Controlled Linux paths improve completion but expose first-byte and fairness costs.
- Live exporter keying and actual QUIC streams isolate small objects without free capacity.
